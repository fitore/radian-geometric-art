# Copy this file to .env and fill in your Anthropic API key.
# The key is embedded at build time — keep .env out of version control.
# If absent, Radian will prompt for a key in the Settings panel at runtime.
VITE_ANTHROPIC_API_KEY=sk-ant-...
